type ShirtPrintProps = {
  imageUrl: string
  prompt: string
  qrCodeUrl: string
  qrCodeText: string
}

export const ShirtPrint = ({ imageUrl, prompt, qrCodeUrl, qrCodeText }: ShirtPrintProps) => {
  imageUrl
  prompt
  qrCodeUrl
  qrCodeText
  //   const element = generatedQrElement()
  return <div style={{ width: "100%", height: "100%", background: "green", display: "flex" }}></div>
}
